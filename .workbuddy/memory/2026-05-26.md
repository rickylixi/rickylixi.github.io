# 2026-05-26

## 网站部署问题排查

- 用户将 `_data/books.yml` 中 Jane Austen 的拼写从 "Austin" 修正为 "Austen"（commit `1c089e6`），但 GitHub Pages 页面未更新
- **根因**：GitHub Actions 工作流未触发（5月26日没有任何运行记录），导致 peaceiris/actions-gh-pages 没有重新构建部署到 gh-pages 分支
- **修复**：在 `.github/workflows/build-blog.yml` 中添加了 `workflow_dispatch` 触发器，方便手动触发部署（commit `3892110`）
- 用户需要自行检查 GitHub 仓库 Actions 设置是否被禁用，然后手动触发构建
