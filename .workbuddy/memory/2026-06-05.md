# 2026-06-05 工作日志

## Lighthouse workflow 警告修复
- 删除了 `lighthouse.yml` 末尾的 `Upload Lighthouse results` 步骤（`actions/upload-artifact@v4`）
- 根因：`treosh/lighthouse-ci-action@v12` 已设 `uploadArtifacts: false`，结果文件根本不写入 `.lighthouseci/`，upload 步骤找不到文件报 warning；同时该 action 触发 Node.js 20 deprecated warning
- 删除后两个 warning 均消除
- commit: `2c31d2a`，push 待用户在终端执行

## convert-images.yml Node.js 24 警告修复
- `upload-artifact@v4`（泛版本指针）在 Node 24 runner 上触发 deprecated warning
- 修复：指定精确版本 `actions/upload-artifact@v4.6.2`（该 patch 版本包含 Node 24 兼容修复）
- 同 job 已有 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`，但泛 `@v4` 指针仍触发警告
- commit: `65e42e9`，push 待用户在终端执行

## 全站 action 版本升级 + .nojekyll

### 根因
- `actions/checkout@v5` 根本不存在（v4 直接跳到 v6），所有三个 workflow 都用了幽灵版本 `@v5`，GitHub 静默降级到 v4 → Node.js 20 deprecated
- `actions/setup-node@v5` 同上
- `actions/upload-artifact@v4.6.2` 有 v7 可用但没用
- GitHub Pages 原生构建在并行运行，与 Gemfile 中的 `gem "jekyll"`（无版本约束）冲突

### 修复
| 文件 | 改动 |
|------|------|
| `build-blog.yml` | `checkout@v5→v6`, `setup-node@v5→v6`, peaceiris 加 `enable_jekyll: false` |
| `convert-images.yml` | `checkout@v5→v6`, `setup-node@v5→v6`, `upload-artifact@v4.6.2→v7` |
| `lighthouse.yml` | `checkout@v5→v6`, `setup-node@v5→v6` |
| `.nojekyll` | 新建空文件，禁用原生 GitHub Pages Jekyll 构建 |

- commit: `f7c4bce`，push 待用户在终端执行
