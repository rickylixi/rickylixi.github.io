# 2026-06-04

## GitHub Actions CI 修复

- 问题1：`lighthouse.yml` 和 `convert-images.yml` 使用了不存在的 `actions/upload-artifact@v6`，修正为 `@v4`
- 问题2：Lighthouse `max-potential-fid` 预算过严（200ms），`research.html` 实测 341ms（第三方脚本贡献），放宽至 400ms
- commit: `c3ff925` "fix: relax max-potential-fid budget to 400ms, fix upload-artifact to v4"
- 本地还有 4 个未提交改动（`build-blog.yml`, `default.html`, `visitor-counter.js`, `accordion.js`），未一起提交
